[workspace]

default-members = ["bot"]
members = ["bot", "web"]
exclude = ["tools/builder"]

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = "abort"
debug = true

[patch.crates-io]
irc = { git = "https://github.com/aatxe/irc", branch = "develop" }
