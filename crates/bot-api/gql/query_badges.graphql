query Badges($login: String!, $channelLogin: String!) {
  user(login: $login) {
    displayBadges(channelLogin: $channelLogin) {
      id
      imageURL
      version
      title
      clickURL
      onClickAction
      description
    }
  }
}